{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build with Bazel",//这个任务的名字在launch.json最后一项配置
            "type": "shell",
            "command": "make build",
            "args": [
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}